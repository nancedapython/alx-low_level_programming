#include <stdio.h>

/**
 * main - prints the alphabet
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char alpha = ';';
while (alpha <=  'z')
{
putchar(alpha);
alpha++;
}
putchar('\n');
return (0);
}

